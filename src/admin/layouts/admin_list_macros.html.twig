{#
 # @package    OSCampus
 # @contact    www.ostraining.com, support@ostraining.com
 # @copyright  2015 Open Source Training, LLC. All rights reserved
 # @license
 #}

{% macro th_checkall() %}
    <th width="1%">
        {{ html('grid.checkall')|raw }}
    </th>
{% endmacro %}

{% macro th_published(field, list_dir, list_order) %}
    <th width="1%" style="min-width:55px" class="nowrap center">
        {{ html('searchtools.sort', 'COM_OSCAMPUS_PUBLISHED', field, list_dir, list_order)|raw }}
    </th>
{% endmacro %}

{% macro th_sort(order_field, title, list_dir, list_order, width = '') %}
    <th class="nowrap" {% if width %}width="{{ width }}"{% endif %}>
        {{ html('searchtools.sort', title, order_field, list_dir, list_order)|raw }}
    </th>
{% endmacro %}

{% macro td_check(index, id) %}
    <td class="center hidden-phone">
        {{ html('grid.id', index, id)|raw }}
    </td>
{% endmacro %}

{% macro td_published(published, index, prefix) %}
    <td class="center">
        <div class="btn-group">
            {{ html('jgrid.published', published, index, prefix)|raw }}
        </div>
    </td>
{% endmacro %}

{% macro td_link(title, link, id, index, checkedout = false, editor_id = 0, checked_out_time = '', prefix = '') %}
    <td class="has-context">
        {% if checkedout %}
            {{ html('jgrid.checkedout', index, editor_id, checked_out_time, prefix, true)|raw }}
        {% endif %}

        <a href="{{ link }}">
            {{ title }}
        </a>
    </td>
{% endmacro %}

{% macro td_edit_link(controller, title, link, id, index, checkedout = false, editor_id = 0, checked_out_time = '', prefix = '') %}
    {% import _self as list %}

    {% set edit_link = 'index.php?option=com_oscampus&task=' ~ controller ~ '.edit&id=' ~ id %}

    {{ list.td_link(title, edit_link, id, index, checkedout, editor_id, checked_out_time, prefix) }}
{% endmacro %}

{% macro td_text(text, class = '') %}
    <td class="{{ class }}">
        {{ text }}
    </td>
{% endmacro %}
